# site/ — the Locveil landing page (GitHub Pages → locveil.com)

Empty until PROD-9 (see `../board/BOARD.md`). Ground rules from D-12:

- **Narrative + routing ONLY** — the joint story, per-product blurbs + screenshots, an
  honest quickstart ("you need a Wiren Board 7, a mic, …"), links into per-repo docs. Never
  duplicates reference material the per-repo docs own.
- The calver **suite manifest** ("Locveil 2026.xx = voice vX + bridge vY + contract vZ +
  images …") is the page's "current release" section, gated on this repo's cross-suites
  passing against exactly those pins (D-11).
- Domains: `locveil.com` is canonical; `.ru` (and `.eu` once registered) 301 to it.
