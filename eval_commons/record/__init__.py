"""Voice-fixture recorder — record conformant 16 kHz/mono/PCM16 WAV fixtures for the
WS-audio system tests. See `docs/design/fixture_recorder.md`.

Entry point: the `eval-fixture-record` console script (`eval_commons.record.cli:main`),
available with the `record` optional extra (`pip install -e '.[record]'`).
"""
